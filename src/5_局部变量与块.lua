---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by luowei.
--- DateTime: 2019/8/12 6:31
--- 局部变量与块
---

print("局部变量")
------------------------------------ 局部变量 ----------------------------------
-- Lua通过local关键字来声明局部变量，局部变量的作用域为声明它的那个块，这种块可以是：
-- 1. 一个函数体，函数体是一个单独的局部变量作用域，里面定义的局部变量在函数体外面都不可见
-- 2. 一个程序块，一个程序块就是位于一对 do end中的代码块
x = 10                  -- 此处定义的变量 x 为全局变量
do
    local x = 5;        -- 局部变量x的作用域为do end代码块内，此时全局变量x在此处不可见（被局部变量x覆盖）
    print("", "do end 块的局部变量 x = " .. x)
end
print("", "全局变量 x = " .. x)

-- 3. 一个控制结构的执行体，如 while 循环体内，if 控制体内。作用域也包括 while 循环的条件判断，如 local x
local i = 1;            -- 局部变量i的作用域为整个文件
while i <= 10 do
    local x = i * 2     -- 局部变量x的作用域为while循环体，此时全局变量x在此处不可见（被局部变量x覆盖）
    if i == 10 then
        print("", "while循环体的局部变量 x = " .. x)
    end
    i = i + 1
end
print("", "全局变量 x = " .. x)

-- 局部变量除了作用域以外，与全局变量类似，也支持多重赋值

